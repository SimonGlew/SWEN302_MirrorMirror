image: node:latest 

cache:
  paths:
   - node_modules/

before_script:
  - npm install -g gulp
  - npm install

stages:
  - build_test

buildwithtests:
  stage: build_test
  script:
   - gulp build
   - npm run test

    