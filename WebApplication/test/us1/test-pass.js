var chai = require('chai');
var expect = chai.expect;

describe('Pass', function(){

	it('This test should pass', function() {
		expect(true).to.equal(true);
	});

	it('This test should also pass', function() {
		expect(true).to.equal(true);
	});

	it('This test should pass too', function() {
		expect(true).to.equal(true);
	});

	it('And this test should pass', function() {
		expect(true).to.equal(true);
	});

	it('This test should superduper pass', function() {
		expect(true).to.equal(true);
	});

});
